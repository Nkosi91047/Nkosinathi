

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Happy Birthday, Bae! 🎂💖</title>
  <meta name="description" content="A tiny birthday website full of love." />
  <link rel="preload" as="audio" href="assets/song.mp3" />
  <style>
    :root {
      --bg: #0f0f14;
      --fg: #ffffff;
      --accent: #d10000; /* Deep red hearts */
      --accent2:#7cf3ff;
      --muted:#c9c9d1;
      --glass: rgba(255,255,255,.08);
      --radius: 16px;
    }

    body {
      margin:0;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial;
      color:var(--fg);
      background:
        radial-gradient(1000px 500px at 10% -10%, #2b1a3a 0%, transparent 60%),
        radial-gradient(800px 500px at 110% 10%, #16283e 0%, transparent 60%),
        linear-gradient(180deg, #0b0b11, #0f0f14);
      overflow-x:hidden;
    }

    /* Floating hearts */
    .heart {
      position:fixed;
      bottom:-40px;
      width:24px;height:24px;
      background: radial-gradient(circle at 30% 30%, #d10000, #a80000 60%);
      transform:rotate(-45deg);
      border-radius:4px 4px 0 0;
      opacity:.7;
      animation: floatUp linear forwards;
      pointer-events:none;
      z-index:1;
    }
    .heart:before,.heart:after{
      content:"";
      position:absolute;
      width:24px;height:24px;
      background:inherit;
      border-radius:50%;
    }
    .heart:before{top:-12px;left:0}
    .heart:after{left:12px;top:0}
    @keyframes floatUp{
      to{
        transform: translateY(-120vh) rotate(-45deg);
        opacity:0;
      }
    }

    /* Beating top hearts */
    .big-heart {
      position: fixed;
      top: 20px;
      font-size: 60px;
      color: #d10000;
      animation: heartbeat 1.2s infinite;
      z-index: 10;
    }
    .big-heart.left { left: 20px; }
    .big-heart.right { right: 20px; }
    @keyframes heartbeat {
      0%, 20%, 40%, 60%, 80%, 100% { transform: scale(1); }
      10%, 30%, 50%, 70%, 90% { transform: scale(1.3); }
    }

    /* Audio control (sticky) */
    .audio-ctrl{
      position:fixed; right:16px; bottom:16px; z-index:999;
      display:flex; gap:8px; align-items:center;
      background:var(--glass); border:1px solid rgba(255,255,255,.12);
      border-radius:999px; padding:8px 12px; backdrop-filter: blur(8px)
    }
    #nowPlaying {
      display: none; /* Hide song title */
    }
  </style>
</head>
<body>

  <!-- Two big beating hearts -->
  <div class="big-heart left">❤️</div>
  <div class="big-heart right">❤️</div>

  <!-- Sticky audio control -->
  <div class="audio-ctrl" id="audioCtrl" hidden>
    <button class="btn" id="playPause">▶️ Play</button>
    <span id="nowPlaying"></span>
  </div>

  <!-- Audio player -->
  <audio id="bgm" preload="auto" loop>
    <source src="assets/song.mp3" type="audio/mpeg" />
    Your browser does not support the audio element.
  </audio>

  <!-- Existing content stays unchanged below this point -->

  <script>
    /************ CONFIG ************/
    const BIRTHDAY_NAME = "Bae";       
    const TARGET_DATE = new Date("2025-08-24T00:00:00");
    /********************************/

    document.getElementById('forName').textContent  = BIRTHDAY_NAME;
    document.getElementById('heroName').textContent = BIRTHDAY_NAME;
    document.getElementById('noteName').textContent = BIRTHDAY_NAME;

    /* Autoplay helper */
    const overlay = document.getElementById('overlay');
    const startBtn = document.getElementById('startBtn');
    const audio = document.getElementById('bgm');
    const audioCtrl = document.getElementById('audioCtrl');
    const playPause = document.getElementById('playPause');

    startBtn.addEventListener('click', async () => {
      overlay.style.display = 'none';
      audioCtrl.hidden = false;
      try {
        await audio.play();
        playPause.textContent = "⏸ Pause";
      } catch(e){
        playPause.textContent = "▶️ Play";
      }
      burstConfetti();
    });

    playPause.addEventListener('click', async () => {
      if(audio.paused){
        try { await audio.play(); playPause.textContent = "⏸ Pause"; }
        catch(e){ playPause.textContent = "▶️ Play"; }
      } else {
        audio.pause();
        playPause.textContent = "▶️ Play";
      }
    });
  </script>
</body>
</html>